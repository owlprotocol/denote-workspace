# Token SDK

## Prerequisites
- Compile `minimal-token` DAR with `daml build`
- Fetch localnet dependencies at root with `pnpm fetch:localnet`
- Run localnet at root with `pnpm start:localnet`

## Run
- `tsx src/uploadDars.ts`
- `tsx src/hello.ts`
